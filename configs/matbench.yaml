task: matbench
model:
  decomposer_backend: openrouter
  decomposer_model: mistralai/mistral-large
  nli_model: cross-encoder/nli-deberta-v3-base
  device: cpu
thresholds:
  support_entailment: 0.55
  refute_contradiction: 0.55
  missing_neutral_max: 0.50
retrieval:
  top_k_sents: 0
run:
  seed: 13
  output_dir: runs
